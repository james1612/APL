<form method="post" action="{{ path('app_image_test_contoller_delete', {'id': image_upload.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image_upload.id) }}">
    <button class="btn">Delete</button>
</form>
